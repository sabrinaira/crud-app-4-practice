/**
 * You can use this space to add extra middleware for the routers
 * if you need to for organizational purposes!
 *
 */

import Model from '../models/model.js';

const controller = {};

Model.add();
Model.get();
Model.update();
Model.delete();

export default controller;
